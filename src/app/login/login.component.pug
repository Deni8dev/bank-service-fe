form.form([formGroup]='loginGroup', (ngSubmit)='login()')
  mat-form-field
    input(matInput, color='primary', placeholder='Username', formControlName='username')
  mat-form-field
    input(matInput, placeholder='Password', formControlName='password')
  div([fxLayout]='"row"', [fxLayoutAlign]='"space-between center"')
    a.letter(mat-button, color='primary', routerLink='.') Register
    button.box(mat-raised-button, color='primary', [disabled]='loginGroup.invalid') Log in
